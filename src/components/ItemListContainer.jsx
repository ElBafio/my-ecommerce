export default function ItemListContainer({ greeting }) {
  return (
    <main className="container">
      <h1>{greeting}</h1>
      <p>Pronto verás nuestro catálogo de productos.</p>
    </main>
  );
}
